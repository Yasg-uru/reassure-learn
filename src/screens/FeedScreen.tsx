import React from 'react';
import { View, Text, Button } from 'react-native';

const FeedScreen: React.FC = () => {
  return (
    <View>
      <Text>Feed Screen</Text>
      <Button title="Example action" onPress={() => {}} />
    </View>
  );
};

export default FeedScreen;
